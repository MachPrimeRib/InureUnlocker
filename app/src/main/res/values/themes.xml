<resources>
    <!-- Base application theme. -->
    <style name="Theme.InureUnlocker" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/inure_background</item>
        <item name="colorPrimaryDark">@color/inure_background</item>
        <item name="colorAccent">@color/inure_accent</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
    </style>
</resources>