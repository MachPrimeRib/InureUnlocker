package app.simple.inureunlocker.viewmodels

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class LicenseKeyVerificationViewModel(application: Application) : AndroidViewModel(application) {

    fun verifyLicenseKey(licenseKey: String) {
        
    }
}